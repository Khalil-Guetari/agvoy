{% extends 'base.html.twig' %}

{% block body %}
	
	<h1>Circuit Programmé : {{programmation.circuit.description}}</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ programmation.circuit.id }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ programmation.circuit.description }}</td>
        </tr>
         <tr>
            <th>Pays Depart</th>
            <td>{{ programmation.circuit.paysDepart }}</td>
        </tr>
         <tr>
            <th>Ville Depart</th>
            <td>{{ programmation.circuit.villeDepart }}</td>
        </tr>
         <tr>
            <th>Ville Arrivee</th>
            <td>{{ programmation.circuit.villeArrivee }}</td>
        </tr>
         <tr>
            <th>Duree Circuit</th>
            <td>{{ programmation.circuit.dureeCircuit }}</td>
        </tr>
    </tbody>
</table>

<table>
  <thead>
    <tr>
        <th>Numéro Etape</th>
        <th>Ville Etape</th>
        <th>Nombre Jours</th>
    </tr>
  </thead>

  {%  for etape in programmation.circuit.etapes %}
  <tr>
    <td>{{ etape.numeroEtape }}</td>
    <td>{{ etape.villeEtape }}</td>
    <td>{{ etape.nombreJours }}</td>
  </tr>
  {% endfor %} {# etapes #}
</table>

{{ dump(programmation) }}

<a href="{{ path('frontoffice_home') }}">retour à l'accueil</a>
	
{% endblock %}

